export enum UserRole {
  ADMIN = "admin",
  MANAGER = "manager",
  STAFF = "staff",
}

export enum TransactionType {
  IMPORT = "import",
  EXPORT = "export",
  TRANSFER = "transfer",
  ADJUSTMENT = "adjustment",
}
